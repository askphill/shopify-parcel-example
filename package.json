{
  "name": "basic",
  "scripts": {
    "start": "shopify theme dev --store shoparcel -x *.css.map",
    "build": "shopify-parcel build",
    "build:dev": "shopify-parcel build:dev",
    "watch": "shopify-parcel watch"
  },
  "shopify-parcel": {
    "entries": [
      "src/*.{js,ts,css}",
      "src/elements/*.{js,ts,css}"
    ]
  },
  "devDependencies": {
    "@askphill/shopify-parcel": "*",
    "tailwindcss": "^3.3.5"
  },
  "dependencies": {
    "@splidejs/splide": "^4.1.4"
  }
}
