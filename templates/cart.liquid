<ul>
  {% for item in cart.items %}
    <li>[{{ item.quantity }}] {{ item.title }} — {{ item.final_line_price | money }}</li>
  {% endfor %}
</ul>

<button
  type="button"
  class="rounded-md bg-white text-black px-4 py-2 mt-4"
  onclick='fetch("/cart/clear", {method: "POST"}).then(data => window.location.reload())'
>
  Clear cart
</button>
